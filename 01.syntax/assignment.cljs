;; Create function `who-is` which accepts person's `name` and `job`
;; and returns a string "Hi! My name is `name` and I am `job`"

;; test
(who-is "John" "CTO") ;; "Hi! My name is John and I am CTO"
